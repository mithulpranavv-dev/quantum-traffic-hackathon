# Core
networkx>=3.2,<4.0
numpy>=1.26,<3.0
scipy>=1.11,<2.0

# Vercel API runtime. Qiskit is optional; qaoa_solver.py has a NumPy
# fallback so the serverless function stays small enough to deploy.
Flask>=3.0,<4.0

# Streamlit dashboard dependencies. Streamlit Cloud installs this root file.
streamlit>=1.32,<2.0
matplotlib>=3.8,<4.0
pandas>=2.1,<3.0
